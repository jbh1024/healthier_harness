spring:
  datasource:
    url: jdbc:mysql://localhost:3306/healthier?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: healthier
    password: healthier
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
